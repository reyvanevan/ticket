# 🎯 System Flow Diagram

## 📊 Complete Integration Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        USER JOURNEY FLOW                             │
└─────────────────────────────────────────────────────────────────────┘

1️⃣ TICKET SELECTION
   ┌──────────────────┐
   │ ticket-detail.html│
   │  - Select qty    │
   │  - View price    │
   └────────┬─────────┘
            │ [Add to cart]
            ↓
   ┌──────────────────┐
   │  LocalStorage    │
   │  umb_order_data  │
   └────────┬─────────┘
            ↓

2️⃣ CHECKOUT
   ┌──────────────────┐
   │  checkout.html   │
   │  - Full name     │
   │  - Email         │
   │  - WhatsApp      │
   │  - ID number     │
   └────────┬─────────┘
            │ [Submit form]
            ↓
   ┌──────────────────┐
   │  LocalStorage    │
   │ complete_order   │
   └────────┬─────────┘
            ↓

3️⃣ PAYMENT
   ┌──────────────────┐
   │  payment.html    │
   │  - Show QRIS     │
   │  - Upload proof  │
   │  - Countdown 24h │
   └────────┬─────────┘
            │ [Upload & Confirm]
            ↓
   ┌──────────────────────────────────────┐
   │        DUAL INTEGRATION              │
   │                                      │
   │  ┌──────────────┐  ┌──────────────┐│
   │  │ save_order   │  │send_ticket   ││
   │  │    .php      │  │  _email.php  ││
   │  └──────┬───────┘  └──────┬───────┘│
   │         │                  │        │
   │         ↓                  ↓        │
   │  ┌──────────┐      ┌──────────┐   │
   │  │  MySQL   │      │   n8n    │   │
   │  │ Database │      │ Webhook  │   │
   │  └──────────┘      └──────┬───┘   │
   │                            │        │
   │                            ↓        │
   │                    ┌──────────┐   │
   │                    │  Email   │   │
   │                    │  SMTP    │   │
   │                    └──────────┘   │
   └──────────────────────────────────┘
            ↓
   ┌──────────────────┐
   │  success.html    │
   │  - E-ticket view │
   │  - QR code       │
   │  - Download btn  │
   └──────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                        ADMIN PANEL FLOW                              │
└─────────────────────────────────────────────────────────────────────┘

   ┌──────────────────┐
   │   admin.html     │
   │  Load on open    │
   └────────┬─────────┘
            │
            ↓
   ┌──────────────────┐
   │  get_orders.php  │
   │  Fetch all orders│
   └────────┬─────────┘
            │
            ↓
   ┌──────────────────┐
   │  MySQL Database  │
   │  SELECT * FROM   │
   │     orders       │
   └────────┬─────────┘
            │
            ↓
   ┌──────────────────┐
   │   admin.js       │
   │  - Render table  │
   │  - Show stats    │
   │  - Filter orders │
   └────────┬─────────┘
            │
            ├───── [Admin clicks "Approve"] ─────┐
            │                                     │
            ↓                                     ↓
   ┌──────────────────┐               ┌──────────────────┐
   │update_order_     │               │  Confirmation    │
   │  status.php      │               │     Dialog       │
   └────────┬─────────┘               └──────────────────┘
            │
            ↓
   ┌──────────────────────────────────────┐
   │     TRANSACTION (MySQL)              │
   │                                      │
   │  1. UPDATE orders                    │
   │     SET status = 'verified'          │
   │                                      │
   │  2. INSERT verification_logs         │
   │     (action, admin, timestamp)       │
   │                                      │
   │  3. COMMIT                           │
   └────────┬─────────────────────────────┘
            │
            ↓
   ┌──────────────────┐
   │send_ticket_email │
   │      .php        │
   │  (Auto trigger)  │
   └────────┬─────────┘
            │
            ↓
   ┌──────────────────┐
   │   n8n Webhook    │
   │  POST /new-order │
   └────────┬─────────┘
            │
            ↓
   ┌──────────────────┐
   │   Send Email     │
   │   (SMTP Node)    │
   │  - To: buyer     │
   │  - E-ticket data │
   └────────┬─────────┘
            │
            ↓
   ┌──────────────────┐
   │  Response Node   │
   │  CORS headers    │
   │  status: success │
   └────────┬─────────┘
            │
            ↓
   ┌──────────────────┐
   │   admin.js       │
   │  - Show success  │
   │  - Reload data   │
   │  - Update stats  │
   └──────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                       DATABASE SCHEMA                                │
└─────────────────────────────────────────────────────────────────────┘

   ┌─────────────────────────────────────┐
   │            ORDERS TABLE             │
   ├─────────────────────────────────────┤
   │ id (PK)                             │
   │ order_number (UNIQUE)               │
   │ full_name                           │
   │ email                               │
   │ phone                               │
   │ id_number                           │
   │ quantity                            │
   │ ticket_price                        │
   │ admin_fee                           │
   │ total                               │
   │ payment_method                      │
   │ status (pending/waiting/verified)   │
   │ proof_file_name                     │
   │ proof_file_url                      │
   │ order_date                          │
   │ created_at                          │
   │ updated_at                          │
   └────────┬────────────────────────────┘
            │
            │ 1:N relationship
            ↓
   ┌─────────────────────────────────────┐
   │          TICKETS TABLE              │
   ├─────────────────────────────────────┤
   │ id (PK)                             │
   │ order_id (FK → orders.id)           │
   │ ticket_number (UNIQUE)              │
   │ ticket_holder_name                  │
   │ status (active/used/expired)        │
   │ qr_code_data                        │
   │ checked_in (BOOLEAN)                │
   │ checked_in_at                       │
   │ created_at                          │
   └────────┬────────────────────────────┘
            │
            │
   ┌────────┴────────────────────────────┐
   │     VERIFICATION_LOGS TABLE         │
   ├─────────────────────────────────────┤
   │ id (PK)                             │
   │ order_id (FK → orders.id)           │
   │ admin_name                          │
   │ action (APPROVED/REJECTED)          │
   │ notes                               │
   │ created_at                          │
   └─────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                       N8N WORKFLOW                                   │
└─────────────────────────────────────────────────────────────────────┘

   Node 1: WEBHOOK
   ┌─────────────────────────────────────┐
   │  Method: POST                       │
   │  Path: /webhook/new-order           │
   │  Auth: None (Public)                │
   │  Expected payload:                  │
   │  {                                  │
   │    "nama": "John Doe",              │
   │    "email": "john@example.com",     │
   │    "kode_tiket": "UMB2025-001"      │
   │  }                                  │
   └────────┬────────────────────────────┘
            ↓
   Node 2: SEND EMAIL (SMTP)
   ┌─────────────────────────────────────┐
   │  Service: Gmail SMTP                │
   │  Host: smtp.gmail.com               │
   │  Port: 465 (SSL/TLS)                │
   │  From: umbandungfest@gmail.com      │
   │  To: {{ $json.body.email }}         │
   │  Subject: E-Ticket UMbandung Fest   │
   │  Body:                              │
   │    - Event details                  │
   │    - Ticket number                  │
   │    - QR code (future)               │
   └────────┬────────────────────────────┘
            ↓
   Node 3: RESPOND TO WEBHOOK
   ┌─────────────────────────────────────┐
   │  Response Type: JSON                │
   │  Body: { "status": "success" }      │
   │  Headers:                           │
   │    Access-Control-Allow-Origin: *   │
   │    Access-Control-Allow-Methods:    │
   │      POST, OPTIONS                  │
   └─────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                    SECURITY & ERROR HANDLING                         │
└─────────────────────────────────────────────────────────────────────┘

✅ Input Validation
   - Email format check
   - Phone number validation (9-13 digits)
   - File size limit (2MB)
   - File type validation (JPG, PNG, PDF)

✅ SQL Injection Prevention
   - All queries use prepared statements
   - mysqli_real_escape_string for user inputs

✅ CORS Handling
   - Headers set in all PHP endpoints
   - n8n response node includes CORS

✅ Error Fallback
   - Admin panel falls back to localStorage
   - Continue on email failure (log error)
   - Transaction rollback on database error

✅ File Upload Security
   - Validate MIME type
   - Check file extension
   - Generate unique filenames
   - Store outside public_html (recommended)


┌─────────────────────────────────────────────────────────────────────┐
│                      DEPLOYMENT CHECKLIST                            │
└─────────────────────────────────────────────────────────────────────┘

□ Import database_schema.sql
□ Update db.php credentials
□ Update n8n webhook URL (2 places)
□ Configure n8n SMTP (App Password)
□ Set CORS headers in n8n
□ Upload all PHP files
□ Test user flow end-to-end
□ Test admin approve/reject
□ Verify email delivery
□ Check database updates
□ Monitor error logs
□ Backup database regularly
